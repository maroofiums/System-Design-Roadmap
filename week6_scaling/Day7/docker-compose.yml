version: "3.9"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    depends_on:
      - redis
      - db

  redis:
    image: redis:latest

  db:
    image: postgres:latest
    environment:
      POSTGRES_DB: ml_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: whynot